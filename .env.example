# Propbase credentials (if using Propbase scraper)
PROPBASE_EMAIL=your-email@example.com
PROPBASE_PASSWORD=your-password

# Proxy URL (if scraping RealT from US)
PROXY_URL=http://your-proxy-server:port
